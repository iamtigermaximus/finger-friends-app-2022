import React from 'react'
import colours from '../../../../colours'

const BgOutterCircle = () => {
  const colour = colours.bgColours.navyBlue

  return <rect x='60' y='60' width='700' height='900' fill={colour} />
}

export default BgOutterCircle
